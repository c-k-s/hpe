node {
        parameters {
          choice(name: 'Continue',
          choices: 'Yes\nNo',
          description: 'Do you want to continue')
        }
        stage('Git Checkout') {

              echo "Begin checkout of the git project"

          }
        }
